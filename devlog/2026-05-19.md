# 2026-05-19 작업일지

## 완료 항목

- **코드 리뷰**: 마지막 커밋(`21178e3` — MainCore 싱글톤 도입 및 MainWindow 코드비하인드 분리) 검토
  - `Initialize()` 재진입 가드 누락, `_eventBusSubscription` 미정리, public 접근 수준 과도 등 지적
- **Rx EventBus 마이그레이션**: `Action<EmpgRow>` 델리게이트 기반 → `Subject<EmpgRow>` / Rx.NET 6.1.0 패턴으로 전면 교체
  - `IProcessEventBus`: `Subscribe/Unsubscribe` 제거 → `IObservable<EmpgRow> AsObservable()` + `void Publish()`
  - `ProcessEventBus`: `Subject<EmpgRow>` 내부 구현, `Dispose()` 에서 `OnCompleted()`
  - .NET 10 호환: `ObserveOnDispatcher()` 미지원 → `ObserveOn(SynchronizationContext.Current!)` 대체
  - `Subject<T>.AsObservable()` 확장 메서드 CS1061 → `_subject` 직접 반환으로 해결
- **MainWindow 코드비하인드 최소화**: `MainWindowViewModel` 신규 도입
  - `TxStatusText`, `TxEfElapsedText` 바인딩 프로퍼티 (`[ObservableProperty]`)
  - `EfVm` 소유, Rx 구독, `Initialize()` / `Shutdown()` 위임
  - 코드비하인드는 `InitializeComponent` + `DataContext` + ContentHost 배선 + 생명주기 위임만 유지
- **ProcessPipelineTestView 공유 EventBus 연결**: 생성자를 `IProcessEventBus sharedEventBus` 수신으로 변경 — 테스트 결과가 MonitoringView에도 반영
- **아키텍처 문서 작성**: `docs/architecture.md` 전면 재작성
  - 전체 레이어 맵, 코드 읽기 순서(10단계), OP230 데이터 흐름 5단계
  - OpMeta 테이블 구조, WriteRegion 계층, EmpgRow 필드 소유 공정 테이블
  - **OP240 추가 체크리스트**: 신규 파일 3개 + 기존 파일 5곳 수정 방법 상세 기술

## 변경 파일

- `docs/architecture.md`
- `src/ConSight.DONGBO.DAQ/AppEvent/IProcessEventBus.cs`
- `src/ConSight.DONGBO.DAQ/AppEvent/ProcessEventBus.cs`
- `src/ConSight.DONGBO.DAQ/MainCore.cs`
- `src/ConSight.DONGBO.DAQ/MainCore.DriverEvent.cs`
- `src/ConSight.DONGBO.DAQ/MainWindow.xaml`
- `src/ConSight.DONGBO.DAQ/MainWindow.xaml.cs`
- `src/ConSight.DONGBO.DAQ/MainWindowViewModel.cs` *(신규)*
- `src/ConSight.DONGBO.DAQ/Views/01_Monitoring/MonitoringViewModel.cs`
- `src/ConSight.DONGBO.DAQ/Views/99_Test/ProcessPipelineTestView.xaml.cs`

## 커밋

- `21178e3` refactor: MainCore 싱글톤 도입 및 MainWindow 코드비하인드 분리 *(세션 시작 시점 HEAD)*
- *(오늘 신규 커밋 없음 — 변경사항 미커밋 상태)*

## 다음 할 일

- 변경사항 빌드 검증 후 커밋: `feat(C3): Rx EventBus 마이그레이션 및 MainWindowViewModel 분리`
- OP240 공정 추가 (`docs/architecture.md` 7절 체크리스트 순서대로)
- 신규 변경 파일에 대한 Unit 테스트 작성 (`/testgen`)
