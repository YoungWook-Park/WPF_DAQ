# 작업일지 2026-05-18

## 작업 내용

### 1. 실행 환경 점검 및 수신 흐름 분석

- PLC 시뮬레이터 → DAQ 앱 실행 순서 확인
  - 시뮬레이터 먼저 기동 (TCP `:5000` 서버 시작) → DAQ 앱 기동 순서 필수
- **모니터 미표시 원인 분석**
  - `ProcessData_Op200` 내 `_eventBus.Publish(row)` 가 `try` 블록 내 DB 작업 **이후**에 위치
  - SQL Server 미가동 시 예외 → `catch` 진입 → `Publish` 미호출 → 모니터 빈 상태
  - `TxStatus.Text = "연결 완료"` 가 시작 시 고정값 → TCP 연결 실패해도 UI 상 정상처럼 보임

### 2. BackUp_Start 비트 수신 진입점 확인

```
시뮬레이터 버튼 → PlcMemory.Write("D2000", data[0]=1)
  ↓ TCP 서버 보관
PlcReadLoop.RunAsync() — 100ms 폴링
  └─ proc[0] == 1 && _prevProc0[i] == 0   ← PlcReadLoop.cs:48  ★ 진입점
       └─ Op200Parser.Parse → ProcessData_Op200 → DB → EventBus.Publish
```

### 3. MainCore 싱글톤 도입 — MainWindow 코드비하인드 분리

기존 레거시 소스(`D:\project\ConSight260113\PJT\DONG_BO\DAQ`) 구조 분석 후 동일 패턴 적용.

**생성 파일:**

| 파일 | 역할 |
|------|------|
| `MainCore.cs` | 싱글톤 — `PlcDriver`, `ControlUnit_DAQ`, `PlcReadLoop`, `EventBus`, `ConnectionString` 보유. `Initialize()` / `Start()` / `Shutdown()` 수명 관리 |
| `MainCore.DriverEvent.cs` | partial — `SubscribeDriverEvents()` + `cFunc_EventBus_ProcessCompleted()` 콜백 |

**수정 파일:**

| 파일 | 변경 내용 |
|------|----------|
| `MainWindow.xaml.cs` | 디바이스 초기화 로직 전량 제거 → `Window_Loaded`에서 `MainCore.Initialize/Start` 위임. UI 배선(`InitViews`)과 상태 콜백만 잔류 |

**적용한 레거시 컨벤션:**
- `public static readonly MainCore Instance = new()` (static readonly 싱글톤)
- `cFunc_` 접두사 — EventBus 콜백 메서드명
- `Log.WriteInformation($"Location=..., Function=..., Action=...")` 로그 형식
- `MainCore.DriverEvent.cs` — 드라이버/이벤트 핸들러를 partial 파일로 분리
- `#region` 블록으로 프로퍼티 섹션 구분

**빌드 결과:** 경고 0 / 오류 0

## 다음 작업 예정

- `ProcessData_Op200` 내 `_eventBus.Publish` 위치 수정 — DB 실패 시에도 모니터에 표시되도록
- TCP 연결 상태를 UI 상태바에 반영 (현재 `TxStatus` 고정값 문제)
- `MainCore` 기반으로 `ProcessPipelineTestView` 의존성 정리
